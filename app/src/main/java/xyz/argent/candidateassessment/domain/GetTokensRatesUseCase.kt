package xyz.argent.candidateassessment.domain

import xyz.argent.candidateassessment.data.model.TokenResponse

class GetTokensRatesUseCase {

   /* operator fun invoke(
        tokens: MutableList<TokenResponse>,
        tokensAddress: List<String>
    ): List<Double> {
        val filteredTokens = tokens.filter { tokensAddress.contains(it.address) }
        return filteredTokens.mapNotNull { it.price?.rate }
    }*/
}